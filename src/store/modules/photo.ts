import { PhotoState } from '@/store/states/photo';

const state: PhotoState = {
    photos: {},
}

export default {
    namespaced: true,
    state,
    // mutations,
    // actions,
    // getters
};
